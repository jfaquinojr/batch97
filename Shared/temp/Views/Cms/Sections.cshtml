@model IEnumerable<batch97.Models.SectionPage>
@inject Piranha.IApi Api
@{
    //ViewBag.Title = Model.Title;
}

    <!-- Page Heading -->
    <h1 class="my-4">Batch 97
      <small>sections</small>
    </h1>


@foreach (var item in Model)
{
    <!-- section -->
      <div class="row">
        <div class="col-md-7">
          <a href="#">
            <img class="img-fluid rounded mb-3 mb-md-0" src="@Url.Content(item.Section.Photo)" alt="">
          </a>
        </div>
        <div class="col-md-5">
          <h3>@item.Section.Name</h3>
          <h5>Adviser: @item.Section.Teacher</h5>
          @*<p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Laudantium veniam exercitationem expedita laborum at voluptate. Labore, voluptates totam at aut nemo deserunt rem magni pariatur quos perspiciatis atque eveniet unde.</p>*@
          @* <a class="btn btn-primary" asp-action="section" asp-route-id="@item.Id">View Students</a> *@
        </div>
      </div>
    <!-- /.row -->
    
    <hr>
}



